/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', '3dellium-payload.vercel.app', 'cms.3dellora.com'],
    },
  };
  
  export default nextConfig;